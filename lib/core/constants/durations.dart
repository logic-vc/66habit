class AnimationDurations {
  // Long press durations
  static const longPressShort = Duration(milliseconds: 1000);
  static const longPressNormal = Duration(milliseconds: 1500);
  static const longPressLong = Duration(milliseconds: 2000);

  // Animation durations
  static const buttonPress = Duration(milliseconds: 100);
  static const gaugeUpdate = Duration(milliseconds: 50);
  static const particleExplosion = Duration(milliseconds: 800);
  static const chainConnection = Duration(milliseconds: 400);
  static const cardSlide = Duration(milliseconds: 300);

  // Haptic feedback intervals
  static const hapticInterval = Duration(milliseconds: 375); // 25%, 50%, 75%
}
